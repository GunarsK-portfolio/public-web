# API URL - Choose one configuration based on your setup:

# Option 1: Via Traefik proxy (full infrastructure stack)
VITE_API_URL=http://localhost/api/v1

# Option 2: Direct to service (for local development)
# VITE_API_URL=http://localhost:8082/api/v1
